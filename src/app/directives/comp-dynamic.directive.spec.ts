import { CompDynamicDirective } from './comp-dynamic.directive';

describe('CompDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new CompDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
